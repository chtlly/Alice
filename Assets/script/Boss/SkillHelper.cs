using UnityEngine;

public class SkillHelper : MonoBehaviour
{
    //스킬에 필요한 것들을 생성하는 클래스
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
